---
description: "Validate and implement a Gummy story"
---

Use `gummy:build` as Aristotle validation: run the state checker, select the next dependency-ready incomplete spec, write or update failing tests first when practical, implement the smallest useful change, run focused tests and acceptance checks, and use Playwright/e2e evidence for UI-facing work before marking checks or specs complete.
