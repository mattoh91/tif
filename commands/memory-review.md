---
description: "Review Tif self-improvement candidates"
---

Use `tif:memory-review` to inspect pending candidates in `~/.tif/memory/<project-slug>/CANDIDATES.jsonl`. Promote only explicitly approved candidates, reject unsafe ones, and run the candidate verification command after promotion.
