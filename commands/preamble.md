---
description: "Generate Sweet session preamble"
---

Use the `sweet:preamble` skill to generate a copy-paste context block for a fresh session. Do not mutate files unless the user explicitly asks.
