---
description: "Generate Cutiepie session preamble"
---

Use the `cutiepie:preamble` skill to generate a copy-paste context block for a fresh session. Do not mutate files unless the user explicitly asks.
